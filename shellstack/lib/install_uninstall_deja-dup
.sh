echo "Will now install install_uninstall_deja-dup
"
	sudo apt-get update
	sleep 2m
	#
	sleep 3
	sudo snap install deja-dup

sudo snap remove deja-dup

	echo "install_uninstall_deja-dup
 has been installed"
	sleep 3
