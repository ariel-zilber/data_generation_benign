echo "Will now install install_uninstall_ondra-snap-ssh-debug
"
	sudo apt-get update
	sleep 2m
	#
	sleep 3
	sudo snap install ondra-snap-ssh-debug

sudo snap remove ondra-snap-ssh-debug

	echo "install_uninstall_ondra-snap-ssh-debug
 has been installed"
	sleep 3
