echo "Will now install install_uninstall_pixbuf-desktop
"
	sudo apt-get update
	sleep 2m
	#
	sleep 3
	sudo snap install pixbuf-desktop

sudo snap remove pixbuf-desktop

	echo "install_uninstall_pixbuf-desktop
 has been installed"
	sleep 3
